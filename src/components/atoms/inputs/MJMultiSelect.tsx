import { MultiSelect, type MultiSelectProps } from '@mantine/core';

type MJProps = MultiSelectProps;

const MJMultiSelect = (props: MJProps) => {
  return <MultiSelect {...props} />;
};

export default MJMultiSelect;
